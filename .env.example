# Kopioi tämä tiedosto nimelle ".env" paikallista kehitystä varten
# (Vercelissä asetetaan Project Settings -> Environment Variables).
# Ks. src/lib/server/api/client.ts

# FISU-API:n perusosoite. Oletusarvo ("https://api2.simu.fi") käytössä
# jos tätä ei aseteta — tätä tarvitsee siis muuttaa vain jos testaat
# esim. paikallista/staging-APIa vasten.
FISU_API_BASE_URL=https://api2.simu.fi
